pipeline {
    agent any

    stages('Build') {
        steps {
            sh 'echo "Hello for the first pipeline from Jenkins"'
            sh '''
                echo "Our first pipeline"
                '''
				ls -la
        }
    }



}